# Development defaults. Mock mode is OFF by default — `npm run dev` expects the
# backend on http://localhost:8080 (see the vite dev proxy).
# Use `npm run dev:mock` (loads .env.mock) for the built-in mock backend, or
# uncomment the next line to force mock mode for plain `npm run dev`:
# VITE_MOCK=1
