# Copy to .env (gitignored) and change every password.
POSTGRES_DB=smarthome
POSTGRES_USER=smarthome
POSTGRES_PASSWORD=change-me-db
MQTT_SERVER_USERNAME=server
MQTT_SERVER_PASSWORD=change-me-mqtt
WEBUI_PORT=80
